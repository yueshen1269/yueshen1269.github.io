---
title: 作用域
date: 2019-07-07 20:51:52
categories: 你不知道的JavaScript
tags:
- 作用域
-
---

# 作用域
作用域是一套存储变量，并且之后可以方便地找到这些变量的规则。

## 编译原理

### 传统编译语言流程:
1. 分词/词法分析(Tokenizing/Lexing)：将字符组成的字符串分解成（对编程语言）有意义的代码块
    `var a = 2` => `var`、`a`、`2`、`;`
2. 解析/语法分析(Parsing): 将词法单元流（数组）转化成一个由元素逐级嵌套所组成的代表程序语法结构的树，这个树被称为“抽象语法树”（Abstract Syntax Tree，AST）
3. 代码生成： 将AST转换为可执行代码块的过程被称为代码生成，主要是将AST转换为一组机器指令

JavaScript的任何代码在执行器都要进行编译（通常在执行前）

## 理解作用域

* 引擎：从头到尾负责整个JavaScript程序的编译及执行过程
* 编译器：引擎的好朋友之一，负责语法分析及代码生成等脏活累活。
* 作用域：引擎的另一位好朋友，负责收集并维护由所有声明的标识符（变量）组成的一系列查询，并实施一套非常严格的规则，确定当前执行的代码对这些标识符的访问权限。（我的理解：作用域有多个层级，每一个作用域管理自己的旗下变量，并严格限制其他变量对自己旗下变量的访问。低层级的作用于可以访问上级作用域管理的变量，高层级不能访问低层级，兄弟层级之间也不不能访问。）

`var a = 2`
1. `var a`: 编译器会询问作用域是否已有一个该名称的变量存在于同一个作用域，存在则忽略该声明,否则会要求在当前作用域内生命一个新变量，并命名为a
2. 接下来编译器会为引擎生成运行时所需代码，这些代码被用来处理`a=2`这个赋值操作。引擎运行时会首先询问作用域，在当前的作用域集合中是否存在一个叫做a的变量，存在则使用这个变量，否则会继续查找该变量。

### 变量的LHS和LRS查询
LHS:
1. 变量出现在左边进行LHS查询
2. LHS查询试图找到变量的容器本身
3. 赋值操作的目标是谁（LHS）

RHS:
1. 变量出现在右边进行RHS查询，并不是右侧，而是非左侧
2. RHS查询相当于查询某个变量的值，
3. 谁是赋值操作的源头（RHS）

```JavaScript
function foo(a) {
  console.log(a);
}
foo(2);
```
其中：LHS有
* a = 2

RHS:
* 调用foo函数是查询foo
* 输出a时查询a
* 查询console

个人理解：LHS和RHS区别：LHS查询只关心在作用域内有没有声明这个变量，不关心它的值；RHS不仅要关心是否声明，还要关心变量的值是否存在，类型等。随便找个杯子喝点水、以及高脚杯内的红酒，咖啡杯内的咖啡，保温杯里的枸杞。

## 作用域嵌套

在当前作用域未找到，则会继续向上层作用域寻找，直到达到全局作用域，查找结束。

## 异常

LHS未找到在非严格模式下不会报ReferenceError，会自动在全局作用域创建一个这样的变量，
RHS未找到会报ReferenceError，找到了但类型不匹配，如一个number类型的变量去调用函数的方法，报TypeError。
